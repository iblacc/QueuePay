server.port=${PORT:8092}
server.servlet.context-path=/api/queuepay/

spring.jpa.hibernate.ddl-auto = update
spring.datasource.url = jdbc:mysql://localhost:3306/queuepay?createDatabaseIfNotExist=true&userSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username = root
spring.datasource.password = S@pph4grace

spring.jpa.show-sql = true

spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

app.jwtSecret = secret

# Expiration in milliseconds - 1 Hour
app.jwtExpirationInMs = 3600000


spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS = false
spring.jackson.time-zone = UTC

management.endpoints.web.cors.allowed-origins=s=*
